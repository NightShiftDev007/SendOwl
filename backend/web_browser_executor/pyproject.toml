[project]
name = "sendowl-web-browser-executor"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "playwright==1.61.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["sendowl_web_browser_executor"]
