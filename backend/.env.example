# LLM API配置（支持 OpenAI SDK 格式的任意 LLM API）
# 推荐在仓库根目录配置 .env；此处仅作备用模板
LLM_API_KEY=your_api_key_here
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODEL_NAME=qwen-plus

# 前置/后置 LLM（Cast Sheet 分角 + 人设/配置终审）
LLM_CAST_PLANNER_MODEL=qwen3.7-plus
# 人设 LLM 终审轮次（默认 0=关闭，本地查重后可 opt-in）；打回重生成人数上限（默认 3）
LLM_PROFILE_REVIEW_ROUNDS=0
LLM_PROFILE_REVIEW_MAX_REGEN=3
# 本地 persona 相似度查重阈值（默认 0.60）
PROFILE_DEDUP_THRESHOLD=0.60
# LLM 并行 worker 数（人设生成 + 配置分批共用；防限流，勿过高）
LLM_PARALLEL_WORKERS=3
LLM_RATE_LIMIT_RETRIES=3
# 初始激活编排：帖子 4-6 条、热点话题至少 3 个（生成端硬约束）
EVENT_INITIAL_POSTS_MIN=4
EVENT_INITIAL_POSTS_MAX=6
EVENT_HOT_TOPICS_MIN=3

# Zep 记忆图谱
ZEP_API_KEY=your_zep_api_key_here

# 高德 Web 服务 Key（可选；GTV Agent 逆地理/周边增强，无 Key 不阻断）
# AMAP_API_KEY=

# 进度总线：memory（默认）| redis（多 worker 可选配置位）
PROGRESS_BUS=memory
# Phase C：优雅退出时不杀模拟子进程
# SIM_DETACH_ON_EXIT=false
